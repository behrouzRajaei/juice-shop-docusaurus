import React from 'react';
import Link from '@docusaurus/Link';
import clsx from 'clsx';
import styles from './index.module.css';

export default function Home() {
  return (
    <div className={styles.container}>
      {/* Header */}
      <header className={styles.header}>
        <img
          src="/img/profile.jpg"
          alt="Your Name"
          className={styles.profileImage}
        />
        <h1>Your Name</h1>
        <p>Software Developer | Security Research | Juice Shop Projects</p>
      </header>

      {/* Projects Section */}
      <section className={styles.projects}>
        <h2>My Projects</h2>
        <div className={styles.projectGrid}>
          {/* Juice Shop Card */}
          <div className={styles.card}>
            <h3>Juice Shop Master</h3>
            <p>Documentation of security challenges in OWASP Juice Shop</p>
            <Link className={styles.button} to="/docs/juice-shop">
              View Project
            </Link>
          </div>

          {/* Add more project cards here if needed */}
        </div>
      </section>
    </div>
  );
}
